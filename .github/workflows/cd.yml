name: Continuous Deployment

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Simulate Deployment
        run: |
          echo "Memulai proses deployment ke Railway..."
          echo "Ups! Ada kesalahan konfigurasi simulasi."
          exit 1 